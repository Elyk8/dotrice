#!/bin/sh
# Creation of latex document
cp -f $XDG_DOCUMENTS_DIR/latextemplate/report/* .

case $1 in
	"") lvim latex-report.tex ;;
	*)
		mv report-template.tex $1.tex
		sed -i "1 s/root = report-template\.tex/root = $1\.tex/" _*.tex
		lvim $1.tex
		;;
esac
